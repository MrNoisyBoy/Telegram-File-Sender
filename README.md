Ничего страшного! Вот README для файла `telegram_sender.py`:

# 🤖 Telegram File Sender

Простой инструмент для отправки содержимого текстовых файлов в Telegram через бота.

## 🚀 Возможности

- 📤 Отправка текстовых файлов в личные чаты, группы и каналы
- 📦 Автоматическое разбиение длинных сообщений (до 4096 символов)
- 🎨 Поддержка Markdown и HTML форматирования
- 🔄 Асинхронная отправка
- 📊 Подробное логирование процесса
- 🔐 Безопасное хранение токена через переменные окружения

## 📋 Требования

- Python 3.7+
- Установленный пакет `aiohttp`

## ⚙️ Установка

```bash
pip install aiohttp
```

## 🤖 Создание бота

1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Получите токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 📝 Использование

### Базовый запуск
```bash
python telegram_sender.py file.txt --chat-id 123456789 --token ваш_токен
```

### Параметры командной строки

| Параметр | Описание | Пример |
|----------|----------|--------|
| `file` | Путь к файлу (обязательно) | `document.txt` |
| `--chat-id`, `-c` | ID чата или @username (обязательно) | `123456789` или `@channel` |
| `--token`, `-t` | Токен бота | `123456:ABCdef` |
| `--parse-mode`, `-p` | Режим форматирования | `HTML`, `Markdown` |
| `--encoding`, `-e` | Кодировка файла | `utf-8`, `cp1251` |

## 🎯 Примеры

### 1. Отправка простого файла
```powershell
python telegram_sender.py message.txt -c 123456789 -t 123456:ABCdef
```

### 2. Использование переменной окружения (безопасно)
```powershell
# Windows PowerShell
$env:TELEGRAM_BOT_TOKEN="ваш_токен"
python telegram_sender.py message.txt -c 123456789

# Linux/Mac
export TELEGRAM_BOT_TOKEN="ваш_токен"
python telegram_sender.py message.txt -c 123456789
```

### 3. Отправка с HTML-форматированием
Создайте файл `announce.html`:
```html
<b>Важное объявление!</b>
<i>Обновление от 16.02.2026</i>
<code>Версия 2.0</code>
```

Отправьте:
```bash
python telegram_sender.py announce.html -c 123456789 -p HTML
```

### 4. Отправка с другой кодировкой
```bash
python telegram_sender.py old_document.txt -c 123456789 -e cp1251
```

## 📱 Как узнать chat_id

1. Напишите боту любое сообщение
2. Выполните в браузере:
```
https://api.telegram.org/botВАШ_ТОКЕН/getUpdates
```
3. Найдите `"chat":{"id":123456789}`

## 📊 Логирование

Скрипт показывает процесс отправки:
```
2026-02-16 21:54:32 - __main__ - INFO - Бот @my_bot подключен
2026-02-16 21:54:34 - __main__ - INFO - Сообщение отправлено (ID: 2)
```

## 🐛 Возможные ошибки

| Ошибка | Решение |
|--------|---------|
| `Файл не найден` | Проверьте путь к файлу |
| `API Error 401` | Неверный токен |
| `API Error 400` | Неверный chat_id или бот не в чате |
| `Invalid start byte` | Укажите правильную кодировку (`--encoding`) |

## 💡 Советы

- Для частого использования сохраните токен в переменную окружения
- Бот должен быть участником чата/канала для отправки
- Для каналов бот должен быть администратором

## 📄 Лицензия

MIT
